{
  "name": "balda-react",
  "private": true,
  "version": "1.0.0",
  "description": "Игра «Балда» (человек против компьютера) — React-версия",
  "type": "module",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc scripts/check.ts --module commonjs --target es2020 --esModuleInterop --skipLibCheck --outDir .tmp-check && node -e \"require('fs').writeFileSync('.tmp-check/package.json', JSON.stringify({type:'commonjs'}))\" && node .tmp-check/scripts/check.js"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.1.0"
  }
}
